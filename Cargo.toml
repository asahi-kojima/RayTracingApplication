[package]
name = "ray_tracing"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
rand = "0.10.1"
sdl2 = {version = "0.38.0", features = ["bundled"]}
serde = { version = "1", features = ["derive"] }
serde_json = "1"